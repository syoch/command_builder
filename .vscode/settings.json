{
  "C_Cpp.default.configurationProvider": "ms-vscode.cmake-tools",
  "saveAndRun": {
    "commands": [
      {
        "cmd": "ninja -C build && ninja -C build test",
        "notMatch": "CMakeLists.txt"
      }
    ]
  }
}